'use client';

import { useEffect, useState } from 'react';
import { BarChart3, Box, ChevronDown, Copy, ExternalLink, Eye, Menu, MousePointer2, Plus, Settings, ShoppingBag, Sparkles, Store, X } from 'lucide-react';
import { createClient } from '@/lib/supabase/client';

const nav = [['Overview', Eye], ['Products', Box], ['Storefront', Store], ['Analytics', BarChart3], ['Settings', Settings]] as const;
type Workspace = { id: string; name: string; slug: string; description: string | null };
type Product = { id: string; name: string; category: string | null; provider: string | null; price: number | null; currency: string | null; is_published: boolean; is_featured: boolean; position: number; };

export default function Dashboard() {
  const [active, setActive] = useState('Overview'); const [mobileOpen, setMobileOpen] = useState(false); const [workspace, setWorkspace] = useState<Workspace | null>(null); const [products, setProducts] = useState<Product[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
  useEffect(() => { (async () => { const s=createClient(); const {data:{user}}=await s.auth.getUser(); if(!user){window.location.href='/login';return;} const {data:m}=await s.from('organization_members').select('organization_id').eq('user_id',user.id).limit(1).maybeSingle(); if(!m?.organization_id){window.location.href='/onboarding';return;} const [{data:o,error:oe},{data:r,error:pe}]=await Promise.all([s.from('organizations').select('id,name,slug,description').eq('id',m.organization_id).single(),s.from('products').select('id,name,category,provider,price,currency,is_published,is_featured,position').eq('organization_id',m.organization_id).order('position')]); if(oe||pe)setError(oe?.message||pe?.message||'Unable to load your workspace.');setWorkspace(o);setProducts(r||[]);setLoading(false); })(); }, []);
  if(loading)return <div className="app-loading"><div className="hub-logo"><Sparkles size={18}/></div><span>Loading your workspace…</span></div>; if(error&&!workspace)return <div className="app-loading"><strong>{error}</strong></div>; const published=products.filter(p=>p.is_published).length; const storefrontPath=`/store/${workspace?.slug||''}`;
  return <div className="app-shell"><aside className={`sidebar ${mobileOpen?'open':''}`}><div className="sidebar-brand"><span className="hub-logo"><Sparkles size={17}/></span><span>Product Hub</span><button className="mobile-close" onClick={()=>setMobileOpen(false)}><X size={20}/></button></div><div className="org-switch"><span>{workspace?.name||'Your workspace'}</span><ChevronDown size={15}/></div><nav className="side-nav">{nav.map(([label,Icon])=><button key={label} className={active===label?'active':''} onClick={()=>{setActive(label);setMobileOpen(false)}}><Icon size={20}/><span>{label}</span></button>)}</nav><div className="plan-card"><div><Sparkles size={17}/><strong>Free Plan</strong></div><span>Start building your storefront.</span><button>View plans <ExternalLink size={13}/></button></div><div className="help"><span>?</span> Need help? <span>→</span></div><div className="user-row"><span className="avatar">{workspace?.name?.slice(0,2).toUpperCase()||'PH'}</span><div><strong>{workspace?.name||'Account'}</strong><small>Owner</small></div><ChevronDown size={16}/></div></aside>{mobileOpen&&<button className="sidebar-overlay" aria-label="Close menu" onClick={()=>setMobileOpen(false)}/>}<main className="main-shell"><header className="topbar"><button className="mobile-menu" onClick={()=>setMobileOpen(true)}><Menu size={22}/></button><div className="topbar-title">{active}</div><div className="topbar-actions"><a href={storefrontPath} className="view-store"><Eye size={17}/> View storefront</a><span className="avatar small">{workspace?.name?.slice(0,2).toUpperCase()||'PH'}</span></div></header><section className="page-content">{active==='Overview'&&<Overview workspace={workspace} products={products} published={published} storefrontPath={storefrontPath}/>} {active==='Products'&&<Products products={products}/>} {active==='Storefront'&&<StorefrontEditor storefrontPath={storefrontPath}/>} {active==='Analytics'&&<Analytics products={products}/>} {active==='Settings'&&<SettingsView workspace={workspace} storefrontPath={storefrontPath}/>}</section></main></div>;
}
function Overview({workspace,products,published,storefrontPath}:{workspace:Workspace|null;products:Product[];published:number;storefrontPath:string}){const[ copied,setCopied]=useState(false);const copy=async()=>{await navigator.clipboard?.writeText(window.location.origin+storefrontPath);setCopied(true);setTimeout(()=>setCopied(false),1400)};return <><div className="page-heading"><div><h1>Good morning <span>👋</span></h1><p>Here&apos;s what&apos;s happening with {workspace?.name||'your storefront'}.</p></div></div><div className="store-url"><div><Eye size={20}/><span>{window.location.origin}{storefrontPath}</span></div><button onClick={copy}><Copy size={16}/>{copied?'Copied':'Copy'}</button><a href={storefrontPath}><ExternalLink size={16}/> View</a></div><div className="metric-grid"><Metric icon={<Eye/>} label="Store views" value="—" delta="No data yet"/><Metric icon={<MousePointer2/>} label="Product clicks" value="—" delta="No data yet"/><Metric icon={<Box/>} label="Products" value={String(products.length)} delta={`${published} published`}/></div><div className="panel top-products"><div className="panel-head"><h2>Products</h2><span>{products.length} total</span></div>{products.length===0?<div className="empty">Add your first product to start building your storefront.</div>:<div className="table"><div className="tr th"><span>#</span><span>Product</span><span>Status</span><span>Price</span></div>{products.slice(0,5).map((p,i)=><div className="tr" key={p.id}><span>{i+1}</span><div className="product-cell"><div><a href={`/dashboard/products/${p.id}`}><strong>{p.name}</strong></a><small>{p.category||'Product'}</small></div></div><span><b className={`status ${p.is_published?'published':'draft'}`}>{p.is_published?'Published':'Draft'}</b></span><span>{p.price==null?'—':`${p.currency||'AOA'} ${p.price}`}</span></div>)}</div>}</div></>}
function Metric({icon,label,value,delta}:{icon:React.ReactNode;label:string;value:string;delta:string}){return <div className="metric"><div className="metric-icon">{icon}</div><span>{label}</span><strong>{value}</strong><small>{delta}</small></div>}
function Products({products}:{products:Product[]}){return <><div className="page-heading product-heading"><div><h1>Products</h1><p>Manage everything that appears in your storefront.</p></div><a className="primary" href="/dashboard/products/new"><Plus size={18}/> Add product</a></div><div className="panel product-list"><div className="list-header"><span>Product</span><span>Category</span><span>Provider</span><span>Status</span><span>Price</span><span></span></div>{products.map(p=><div className="product-row" key={p.id}><div><a href={`/dashboard/products/${p.id}`}><strong>{p.name}</strong></a></div><span>{p.category||'—'}</span><span>{p.provider||'—'}</span><span><b className={`status ${p.is_published?'published':'draft'}`}>{p.is_published?'Published':'Draft'}</b></span><span>{p.price==null?'—':`${p.currency||'AOA'} ${p.price}`}</span><a className="row-action" href={`/dashboard/products/${p.id}`}>Edit</a></div>)}{!products.length&&<div className="empty">No products yet. <a href="/dashboard/products/new">Add your first product.</a></div>}</div></>}
function StorefrontEditor({storefrontPath}:{storefrontPath:string}){return <><div className="page-heading"><div><h1>Storefront</h1><p>Customize the experience your audience sees.</p></div><a className="primary" href={storefrontPath}><Eye size={18}/> Preview</a></div><div className="panel editor-placeholder"><h2>Your storefront is ready to customize</h2><p>Use the preview to see your published products. Advanced visual controls will be connected here next.</p><a className="primary" href={storefrontPath}>Open storefront</a></div></>}
function Analytics({products}:{products:Product[]}){return <><div className="page-heading"><div><h1>Analytics</h1><p>Track performance as visitors interact with your storefront.</p></div></div><div className="metric-grid four"><Metric icon={<Eye/>} label="Store views" value="—" delta="Waiting for visits"/><Metric icon={<ShoppingBag/>} label="Product views" value="—" delta="Waiting for visits"/><Metric icon={<MousePointer2/>} label="Product clicks" value="—" delta="Waiting for clicks"/><Metric icon={<BarChart3/>} label="CTR" value="—" delta="Waiting for data"/></div><div className="panel empty analytics-empty"><BarChart3 size={28}/><h2>Analytics will appear here</h2><p>Once people visit your storefront, Product Hub will start showing performance data.</p><span>{products.length} products currently configured</span></div></>}
function SettingsView({workspace,storefrontPath}:{workspace:Workspace|null;storefrontPath:string}){return <><div className="page-heading"><div><h1>Settings</h1><p>Manage your brand, account and storefront preferences.</p></div></div><div className="settings-grid"><div className="panel setting-card"><h2>Brand profile</h2><label>Brand name<input defaultValue={workspace?.name||''}/></label><label>Username<input defaultValue={workspace?.slug||''}/></label><label>Bio<textarea defaultValue={workspace?.description||''}/></label><button className="primary">Save changes</button></div><div className="panel setting-card"><h2>Storefront link</h2><p>Share this link everywhere you want people to discover your products.</p><div className="copy-field"><span>{typeof window!=='undefined'?window.location.origin:''}{storefrontPath}</span></div></div></div></>}
