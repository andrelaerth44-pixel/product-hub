'use client';

import { ReactNode, useEffect, useMemo, useState } from 'react';
import { Activity, BarChart3, ExternalLink, LayoutDashboard, Package, Settings, Store, Plus, Search, LogOut, Copy, Check, Users } from 'lucide-react';
import { createClient } from '@/lib/supabase/client';
import { DashboardCustomizer } from '@/components/dashboard-customizer';
import { DashboardHero } from '@/components/dashboard-hero';

export default function DashboardPage() {
  const supabase = createClient(); const [user,setUser]=useState<any>(null); const [org,setOrg]=useState<any>(null); const [products,setProducts]=useState<any[]>([]); const [events,setEvents]=useState<any[]>([]); const [storefront,setStorefront]=useState<any>(null); const [active,setActive]=useState('overview'); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); const [copied,setCopied]=useState(false); const [range,setRange]=useState(30);
  useEffect(()=>{let mounted=true;(async()=>{try{const{data:auth}=await supabase.auth.getUser();if(!auth.user)return;const{data:member}=await supabase.from('organization_members').select('organization_id').eq('user_id',auth.user.id).limit(1).maybeSingle();if(!member?.organization_id){setError('Ainda não tens uma organização configurada.');return}const[{data:organization},{data:items},{data:shop}]=await Promise.all([supabase.from('organizations').select('*').eq('id',member.organization_id).single(),supabase.from('products').select('*').eq('organization_id',member.organization_id).order('created_at',{ascending:false}),supabase.from('storefronts').select('*').eq('organization_id',member.organization_id).limit(1).maybeSingle()]);const since=new Date(Date.now()-90*86400000).toISOString();const{data:analytics}=await supabase.from('analytics_events').select('*').eq('organization_id',member.organization_id).gte('created_at',since).order('created_at',{ascending:true});if(!mounted)return;setUser(auth.user);setOrg(organization);setProducts(items||[]);setStorefront(shop);setEvents(analytics||[])}catch(e:any){if(mounted)setError(e?.message||'Não foi possível carregar o dashboard.')}finally{if(mounted)setLoading(false)}})();return()=>{mounted=false}},[]);
  const filteredEvents=useMemo(()=>events.filter(e=>Date.now()-new Date(e.created_at).getTime()<=range*86400000),[events,range]); const views=filteredEvents.filter(e=>e.event_type==='view'||e.event_type==='page_view').length; const clicks=filteredEvents.filter(e=>e.event_type==='click'||e.event_type==='product_click').length; const productViews=filteredEvents.filter(e=>e.event_type==='product_view').length; const ctr=views?(clicks/views)*100:0;
  const nav=[['overview','Visão geral',LayoutDashboard],['products','Produtos',Package],['storefront','Vitrine',Store],['analytics','Analytics',BarChart3],['settings','Definições',Settings]] as const;
  function copyStoreUrl(){if(!storefront?.slug)return;navigator.clipboard?.writeText(`${window.location.origin}/store/${storefront.slug}`);setCopied(true);setTimeout(()=>setCopied(false),1500)}
  if(loading)return <div className="app-shell"><main className="main-shell"><div className="page-content"><div className="empty">A preparar o teu espaço…</div></div></main></div>; if(error)return <div className="app-shell"><main className="main-shell"><div className="page-content"><div className="empty">{error}</div></div></main></div>;
  return <div className="app-shell"><aside className="sidebar"><div className="sidebar-brand"><div className="hub-logo">P</div><span>Product Hub</span></div><div className="org-switch"><span>{org?.name||'Workspace'}</span><Users size={14}/></div><nav className="side-nav">{nav.map(([key,label,Icon])=><button key={key} className={active===key?'active':''} onClick={()=>setActive(key)}><Icon size={17}/><span>{label}</span></button>)}</nav><div className="plan-card"><div><Activity size={15}/><strong>Workspace ativo</strong></div><span>Dados e operações num único lugar.</span></div><div className="help"><span>?</span> Ajuda <LogOut size={14}/></div><div className="user-row"><div className="avatar">{(user?.email?.[0]||'U').toUpperCase()}</div><div><strong>{user?.email?.split('@')[0]||'Utilizador'}</strong><small>{user?.email||''}</small></div></div></aside><main className="main-shell"><header className="topbar"><span className="topbar-title">{active==='overview'?'Visão geral':nav.find(n=>n[0]===active)?.[1]}</span><div className="topbar-actions">{storefront?.slug&&<a className="view-store" href={`/store/${storefront.slug}`} target="_blank" rel="noreferrer"><ExternalLink size={14}/> Abrir vitrine</a>}</div></header><div className="page-content">{active==='overview'&&<><DashboardHero name={user?.user_metadata?.name||user?.email?.split('@')[0]} views={views} clicks={clicks} products={products.length}/>{storefront?.slug&&<div className="store-url"><div><Store size={16}/><span>{window.location.origin}/store/{storefront.slug}</span></div><div><button onClick={copyStoreUrl}>{copied?<Check size={14}/>:<Copy size={14}/>} {copied?'Copiado':'Copiar'}</button><a href={`/store/${storefront.slug}`} target="_blank" rel="noreferrer"><ExternalLink size={14}/> Visualizar</a></div></div>}<div className="metric-grid"><Metric icon={<Activity size={16}/>} label="Visitas" value={views.toLocaleString('pt-PT')} hint={`${range} dias`}/><Metric icon={<BarChart3 size={16}/>} label="Cliques" value={clicks.toLocaleString('pt-PT')} hint="Interações"/><Metric icon={<Package size={16}/>} label="Produtos" value={products.length.toLocaleString('pt-PT')} hint="Catálogo atual"/></div><div className="panel"><div className="panel-head"><h2>Produtos recentes</h2><button onClick={()=>setActive('products')}>Ver todos →</button></div><div className="table">{products.slice(0,6).map(p=><div className="tr" key={p.id}><div>{p.sku||'—'}</div><div className="product-cell"><div className="avatar small"><Package size={15}/></div><div><strong>{p.name}</strong><small>{p.category||'Sem categoria'}</small></div></div><div>{p.price!=null?`${Number(p.price).toLocaleString('pt-PT')} ${p.currency||'AOA'}`:'—'}</div><div>{p.stock??0}</div><div><span className={`status ${p.status==='published'?'published':'draft'}`}>{p.status==='published'?'Publicado':'Rascunho'}</span></div></div>)}{products.length===0&&<div className="empty">Ainda não tens produtos.</div>}</div></div></>}{active==='products'&&<Products products={products}/>} {active==='storefront'&&storefront?.slug&&org?.id&&<DashboardCustomizer organizationId={org.id} storefrontPath={`/store/${storefront.slug}`}/>} {active==='analytics'&&<Analytics events={events} range={range} setRange={setRange} views={views} productViews={productViews} clicks={clicks} ctr={ctr}/>} {active==='settings'&&<SettingsPanel org={org} supabase={supabase}/>}</div></main></div>
}
function Metric({icon,label,value,hint}:{icon:ReactNode;label:string;value:string;hint:string}){return <div className="metric"><div className="metric-icon">{icon}</div><span>{label}</span><strong>{value}</strong><small>{hint}</small></div>}
function Products({products}:{products:any[]}){const[q,setQ]=useState('');const list=products.filter(p=>String(p.name||'').toLowerCase().includes(q.toLowerCase()));return <><div className="page-heading product-heading"><div><h1>Produtos</h1><p>O teu catálogo, organizado e pronto para vender.</p></div><a className="primary" href="/dashboard/products/new"><Plus size={15}/> Adicionar produto</a></div><div className="search"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Pesquisar produto…"/></div><div className="panel" style={{marginTop:16}}><div className="product-list"><div className="list-header"><span>Produto</span><span>Preço</span><span>Stock</span><span>Estado</span><span>SKU</span><span>Atualizado</span><span/></div>{list.map(p=><div className="product-row" key={p.id}><div className="product-cell"><div className="avatar small"><Package size={15}/></div><div><strong>{p.name}</strong><small>{p.category||'Sem categoria'}</small></div></div><span>{p.price!=null?Number(p.price).toLocaleString('pt-PT'):'—'} {p.currency||'AOA'}</span><span>{p.stock??0}</span><span className={`status ${p.status==='published'?'published':'draft'}`}>{p.status==='published'?'Publicado':'Rascunho'}</span><span>{p.sku||'—'}</span><span>{p.updated_at?new Date(p.updated_at).toLocaleDateString('pt-PT'):'—'}</span><button className="more">•••</button></div>)}{list.length===0&&<div className="empty">Nenhum produto encontrado.</div>}</div></div></>}
function Analytics({events,range,setRange,views,productViews,clicks,ctr}:any){const buckets=Array.from({length:7},(_,i)=>{const end=Date.now()-(6-i)*Math.ceil(range/7)*86400000;const start=end-Math.ceil(range/7)*86400000;return{label:new Date(end).toLocaleDateString('pt-PT',{day:'2-digit',month:'short'}),value:events.filter((e:any)=>{const t=new Date(e.created_at).getTime();return t>=start&&t<end}).length}});const max=Math.max(1,...buckets.map(b=>b.value));return <><div className="page-heading"><div><h1>Analytics</h1><p>Comportamento real da tua vitrine.</p></div><div className="tabs">{[7,30,90].map(n=><button key={n} className={range===n?'selected':''} onClick={()=>setRange(n)}>{n} dias</button>)}</div></div><div className="metric-grid four"><Metric icon={<Activity size={16}/>} label="Visitas" value={String(views)} hint="Page views"/><Metric icon={<Package size={16}/>} label="Visualizações de produto" value={String(productViews)} hint="Produtos vistos"/><Metric icon={<BarChart3 size={16}/>} label="Cliques" value={String(clicks)} hint="Interações"/><Metric icon={<Activity size={16}/>} label="CTR" value={`${ctr.toFixed(1)}%`} hint="Cliques / visitas"/></div><div className="panel" style={{padding:'24px 22px',marginTop:18}}><div className="panel-head" style={{padding:'0 0 18px'}}><h2>Atividade</h2><span style={{fontSize:11,color:'#7d8694'}}>Eventos por período</span></div><div style={{height:220,display:'flex',alignItems:'end',gap:12,padding:'18px 4px 4px'}}>{buckets.map((b,i)=><div key={i} style={{flex:1,height:'100%',display:'flex',flexDirection:'column',justifyContent:'end',gap:8}}><div title={`${b.value} eventos`} style={{height:`${Math.max(6,(b.value/max)*100)}%`,borderRadius:8,background:'linear-gradient(180deg,#9bb3ff,#5278e8)',boxShadow:'0 8px 30px rgba(82,120,232,.2)',transition:'height .45s ease'}}/><small style={{textAlign:'center',fontSize:9,color:'#737c8a'}}>{b.label}</small></div>)}</div></div></>}
function SettingsPanel({org,supabase}:any){const[name,setName]=useState(org?.name||'');const[description,setDescription]=useState(org?.description||'');const[saved,setSaved]=useState(false);async function save(){await supabase.from('organizations').update({name,description}).eq('id',org.id);setSaved(true);setTimeout(()=>setSaved(false),1500)}return <><div className="page-heading"><div><h1>Definições</h1><p>Identidade e informação do teu workspace.</p></div></div><div className="panel" style={{padding:24}}><label style={{display:'grid',gap:8,fontSize:12,color:'#8a92a0'}}>Nome do workspace<input value={name} onChange={e=>setName(e.target.value)} style={{background:'rgba(255,255,255,.04)',border:'1px solid rgba(255,255,255,.08)',borderRadius:10,padding:'12px',color:'#eef1f6'}}/></label><label style={{display:'grid',gap:8,fontSize:12,color:'#8a92a0',marginTop:18}}>Descrição<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4} style={{background:'rgba(255,255,255,.04)',border:'1px solid rgba(255,255,255,.08)',borderRadius:10,padding:'12px',color:'#eef1f6',resize:'vertical'}}/></label><button className="primary" onClick={save} style={{marginTop:18}}>{saved?'Guardado':'Guardar alterações'}</button></div></>}
