'use client';

import type { ReactNode } from 'react';
import { useEffect, useMemo, useState } from 'react';
import { BarChart3, Box, ChevronDown, Copy, ExternalLink, Eye, Menu, MousePointer2, Plus, Settings as SettingsIcon, ShoppingBag, Sparkles, Store, X } from 'lucide-react';
import { createClient } from '@/lib/supabase/client';
import { DashboardCustomizer } from '@/components/dashboard-customizer';

type Workspace={id:string;name:string;slug:string;description:string|null};
type Product={id:string;name:string;category:string|null;provider:string|null;price:number|null;currency:string|null;is_published:boolean;is_featured:boolean;position:number};
type Event={event_type:'store_view'|'product_view'|'product_click';product_id:string|null;created_at:string;device_type:string|null};
const nav=[['Overview',Eye],['Products',Box],['Storefront',Store],['Analytics',BarChart3],['Settings',SettingsIcon]] as const;

export default function Dashboard(){
 const [active,setActive]=useState('Overview'),[mobileOpen,setMobileOpen]=useState(false),[workspace,setWorkspace]=useState<Workspace|null>(null),[products,setProducts]=useState<Product[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
 useEffect(()=>{let cancelled=false;async function load(){const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){window.location.href='/login';return;}const {data:m,error:me}=await s.from('organization_members').select('organization_id').eq('user_id',user.id).limit(1).maybeSingle();if(me){if(!cancelled){setError(me.message);setLoading(false);}return;}if(!m?.organization_id){window.location.href='/onboarding';return;}const [o,r]=await Promise.all([s.from('organizations').select('id,name,slug,description').eq('id',m.organization_id).single(),s.from('products').select('id,name,category_id,provider,price,currency,is_published,is_featured,position,categories(name)').eq('organization_id',m.organization_id).order('position',{ascending:true})]);if(cancelled)return;if(o.error||r.error)setError(o.error?.message||r.error?.message||'Unable to load your workspace.');setWorkspace(o.data as Workspace|null);setProducts(((r.data||[]) as any[]).map(p=>({id:p.id,name:p.name,category:p.categories?.name||null,provider:p.provider,price:p.price,currency:p.currency,is_published:p.is_published,is_featured:p.is_featured,position:p.position})));setLoading(false);}load();return()=>{cancelled=true;};},[]);
 if(loading)return <div className="app-loading"><div className="hub-logo"><Sparkles size={18}/></div><span>Loading your workspace…</span></div>;
 if(error&&!workspace)return <div className="app-loading"><strong>{error}</strong></div>;
 const published=products.filter(p=>p.is_published).length,storefrontPath=`/store/${workspace?.slug||''}`;
 return <div className="app-shell"><aside className={`sidebar ${mobileOpen?'open':''}`}><div className="sidebar-brand"><span className="hub-logo"><Sparkles size={17}/></span><span>Product Hub</span><button className="mobile-close" onClick={()=>setMobileOpen(false)} aria-label="Close menu"><X size={20}/></button></div><div className="org-switch"><span>{workspace?.name||'Your workspace'}</span><ChevronDown size={15}/></div><nav className="side-nav" aria-label="Dashboard navigation">{nav.map(([label,Icon])=><button key={label} className={active===label?'active':''} onClick={()=>{setActive(label);setMobileOpen(false)}}><Icon size={20}/><span>{label}</span></button>)}</nav><div className="plan-card"><div><Sparkles size={17}/><strong>Free Plan</strong></div><span>Start building your storefront.</span><button type="button">View plans <ExternalLink size={13}/></button></div><div className="help"><span>?</span> Need help? <span>→</span></div><div className="user-row"><span className="avatar">{workspace?.name?.slice(0,2).toUpperCase()||'PH'}</span><div><strong>{workspace?.name||'Account'}</strong><small>Owner</small></div><ChevronDown size={16}/></div></aside>{mobileOpen&&<button className="sidebar-overlay" aria-label="Close menu" onClick={()=>setMobileOpen(false)}/>}<main className="main-shell"><header className="topbar"><button className="mobile-menu" onClick={()=>setMobileOpen(true)} aria-label="Open menu"><Menu size={22}/></button><div className="topbar-title">{active}</div><div className="topbar-actions"><a href={storefrontPath} className="view-store"><Eye size={17}/> View storefront</a><span className="avatar small">{workspace?.name?.slice(0,2).toUpperCase()||'PH'}</span></div></header><section className="page-content">{active==='Overview'&&<Overview workspace={workspace} products={products} published={published} storefrontPath={storefrontPath}/>} {active==='Products'&&<Products products={products}/>} {active==='Storefront'&&workspace&&<DashboardCustomizer organizationId={workspace.id} storefrontPath={storefrontPath}/>} {active==='Analytics'&&workspace&&<Analytics products={products} organizationId={workspace.id}/>} {active==='Settings'&&workspace&&<SettingsView workspace={workspace} setWorkspace={setWorkspace} storefrontPath={storefrontPath}/>}</section></main></div>;
}

function Overview({workspace,products,published,storefrontPath}:{workspace:Workspace|null;products:Product[];published:number;storefrontPath:string}){
 const [copied,setCopied]=useState(false),[stats,setStats]=useState({views:0,clicks:0});
 useEffect(()=>{if(!workspace)return;const workspaceId=workspace.id;let cancelled=false;async function load(){const s=createClient(),since=new Date(Date.now()-30*86400000).toISOString();const {data}=await s.from('analytics_events').select('event_type').eq('organization_id',workspaceId).gte('created_at',since);if(cancelled)return;setStats({views:(data||[]).filter(e=>e.event_type==='store_view').length,clicks:(data||[]).filter(e=>e.event_type==='product_click').length});}load();return()=>{cancelled=true;};},[workspace]);
 async function copyStorefront(){await navigator.clipboard?.writeText(window.location.origin+storefrontPath);setCopied(true);window.setTimeout(()=>setCopied(false),1400);}
 return <><div className="page-heading"><div><h1>Good morning</h1><p>Here&apos;s what&apos;s happening with {workspace?.name||'your storefront'}.</p></div></div><div className="store-url"><div><Eye size={20}/><span>{typeof window!=='undefined'?window.location.origin:''}{storefrontPath}</span></div><button onClick={copyStorefront} type="button"><Copy size={16}/> {copied?'Copied':'Copy'}</button><a href={storefrontPath}><ExternalLink size={16}/> View</a></div><div className="metric-grid"><Metric icon={<Eye/>} label="Store views" value={String(stats.views)} delta="Last 30 days"/><Metric icon={<MousePointer2/>} label="Product clicks" value={String(stats.clicks)} delta="External purchase clicks"/><Metric icon={<Box/>} label="Products" value={String(products.length)} delta={`${published} published`}/></div><div className="panel top-products"><div className="panel-head"><h2>Products</h2><span>{products.length} total</span></div>{products.length===0?<div className="empty">Add your first product to start building your storefront.</div>:<div className="table"><div className="tr th"><span>#</span><span>Product</span><span>Status</span><span>Price</span></div>{products.slice(0,5).map((p,i)=><div className="tr" key={p.id}><span>{i+1}</span><div className="product-cell"><div><a href={`/dashboard/products/${p.id}`}><strong>{p.name}</strong></a><small>{p.category||'Product'}</small></div></div><span><b className={`status ${p.is_published?'published':'draft'}`}>{p.is_published?'Published':'Draft'}</b></span><span>{p.price==null?'—':`${p.currency||'AOA'} ${p.price}`}</span></div>)}</div>}</div></>;
}
function Metric({icon,label,value,delta}:{icon:ReactNode;label:string;value:string;delta:string}){return <div className="metric"><div className="metric-icon">{icon}</div><span>{label}</span><strong>{value}</strong><small>{delta}</small></div>}
function Products({products}:{products:Product[]}){return <><div className="page-heading product-heading"><div><h1>Products</h1><p>Manage everything that appears in your storefront.</p></div><a className="primary" href="/dashboard/products/new"><Plus size={18}/> Add product</a></div><div className="panel product-list"><div className="list-header"><span>Product</span><span>Category</span><span>Provider</span><span>Status</span><span>Price</span><span/></div>{products.map(p=><div className="product-row" key={p.id}><div><a href={`/dashboard/products/${p.id}`}><strong>{p.name}</strong></a></div><span>{p.category||'—'}</span><span>{p.provider||'—'}</span><span><b className={`status ${p.is_published?'published':'draft'}`}>{p.is_published?'Published':'Draft'}</b></span><span>{p.price==null?'—':`${p.currency||'AOA'} ${p.price}`}</span><a className="row-action" href={`/dashboard/products/${p.id}`}>Edit</a></div>)}{!products.length&&<div className="empty">No products yet. <a href="/dashboard/products/new">Add your first product.</a></div>}</div></>}
function Analytics({products,organizationId}:{products:Product[];organizationId:string}){const [events,setEvents]=useState<Event[]>([]),[loading,setLoading]=useState(true),[range,setRange]=useState(30),[error,setError]=useState('');useEffect(()=>{let cancelled=false;async function load(){setLoading(true);setError('');const s=createClient(),since=new Date(Date.now()-range*86400000).toISOString(),{data,error:e}=await s.from('analytics_events').select('event_type,product_id,created_at,device_type').eq('organization_id',organizationId).gte('created_at',since).order('created_at',{ascending:true});if(cancelled)return;if(e)setError(e.message);setEvents((data as Event[])||[]);setLoading(false);}load();return()=>{cancelled=true;};},[organizationId,range]);const views=events.filter(e=>e.event_type==='store_view').length,productViews=events.filter(e=>e.event_type==='product_view').length,clicks=events.filter(e=>e.event_type==='product_click').length,ctr=views?clicks/views*100:0;const top=useMemo(()=>products.map(p=>({product:p,count:events.filter(e=>e.product_id===p.id&&e.event_type==='product_click').length})).filter(x=>x.count>0).sort((a,b)=>b.count-a.count).slice(0,5),[events,products]);const days=Array.from({length:Math.min(range,14)},(_,i)=>{const d=new Date(Date.now()-(Math.min(range,14)-1-i)*86400000);return{label:d.toLocaleDateString('pt-PT',{day:'2-digit',month:'2-digit'}),value:events.filter(e=>e.event_type==='store_view'&&new Date(e.created_at).toDateString()===d.toDateString()).length};}),max=Math.max(1,...days.map(d=>d.value));return <><div className="page-heading"><div><h1>Analytics</h1><p>Track performance as visitors interact with your storefront.</p></div><select value={range} onChange={e=>setRange(Number(e.target.value))}><option value={7}>Last 7 days</option><option value={30}>Last 30 days</option><option value={90}>Last 90 days</option></select></div><div className="metric-grid four"><Metric icon={<Eye/>} label="Store views" value={loading?'…':String(views)} delta={`${range}-day period`}/><Metric icon={<ShoppingBag/>} label="Product views" value={loading?'…':String(productViews)} delta="Product detail visits"/><Metric icon={<MousePointer2/>} label="Product clicks" value={loading?'…':String(clicks)} delta="External purchase clicks"/><Metric icon={<BarChart3/>} label="CTR" value={loading?'…':`${ctr.toFixed(1)}%`} delta="Clicks ÷ store views"/></div>{error&&<div className="panel empty"><strong>{error}</strong></div>}<div className="panel analytics-chart"><div className="panel-head"><div><h2>Store views</h2><span>Daily traffic</span></div><strong>{views} total</strong></div>{events.length===0?<div className="empty"><BarChart3 size={28}/><h2>No visits yet</h2><p>Share your storefront to start collecting real analytics.</p></div>:<div className="mini-chart">{days.map(d=><div className="bar-col" key={d.label}><span className="bar-value">{d.value}</span><div className="bar" style={{height:`${Math.max(6,d.value/max*150)}px`}}/><small>{d.label}</small></div>)}</div>}</div><div className="panel top-products"><div className="panel-head"><h2>Top products</h2><span>Clicks</span></div>{top.length===0?<div className="empty">No product clicks in this period.</div>:<div className="table">{top.map((x,i)=><div className="tr" key={x.product.id}><span>{i+1}</span><div className="product-cell"><div><strong>{x.product.name}</strong><small>{x.product.provider||x.product.category||'Product'}</small></div></div><span>{x.count} click{x.count===1?'':'s'}</span></div>)}</div>}</div></>}
function SettingsView({workspace,setWorkspace,storefrontPath}:{workspace:Workspace;setWorkspace:(w:Workspace)=>void;storefrontPath:string}){const [name,setName]=useState(workspace.name),[description,setDescription]=useState(workspace.description||''),[saving,setSaving]=useState(false),[saved,setSaved]=useState(false),[error,setError]=useState('');useEffect(()=>{setName(workspace.name);setDescription(workspace.description||'');},[workspace.id,workspace.name,workspace.description]);async function save(){if(name.trim().length<2){setError('Brand name must be at least 2 characters.');return;}setSaving(true);setSaved(false);setError('');const s=createClient(),{data,error:e}=await s.from('organizations').update({name:name.trim(),description:description.trim()||null}).eq('id',workspace.id).select('id,name,slug,description').single();if(e)setError(e.message);else if(data){setWorkspace(data as Workspace);setSaved(true);window.setTimeout(()=>setSaved(false),1800);}setSaving(false);}return <><div className="page-heading"><div><h1>Settings</h1><p>Manage your brand and storefront preferences.</p></div></div>{error&&<div className="panel empty" style={{color:'#9a2f2f'}}>{error}</div>}<div className="settings-grid"><div className="panel setting-card"><h2>Brand profile</h2><label>Brand name<input value={name} onChange={e=>setName(e.target.value)} minLength={2}/></label><label>Username<input value={workspace.slug} readOnly/></label><label>Bio<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4}/></label><button className="primary" onClick={save} disabled={saving} type="button">{saved?'Saved ✓':saving?'Saving…':'Save changes'}</button></div><div className="panel setting-card"><h2>Storefront link</h2><p>Share this link everywhere you want people to discover your products.</p><div className="copy-field"><span>{typeof window!=='undefined'?window.location.origin:''}{storefrontPath}</span></div></div></div></>}
