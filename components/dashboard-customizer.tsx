'use client';

import { useEffect, useState } from 'react';
import { Check, Eye, LayoutGrid, Save, Type } from 'lucide-react';
import { createClient } from '@/lib/supabase/client';

type Theme = { mode: 'light' | 'soft' | 'dark'; accent: string; radius: 'small' | 'medium' | 'large'; layout: 'grid' | 'list'; showPrices: boolean };
const defaults: Theme = { mode: 'light', accent: '#1f1f1f', radius: 'medium', layout: 'grid', showPrices: true };

export function DashboardCustomizer({ organizationId, storefrontPath }: { organizationId: string; storefrontPath: string }) {
  const [theme, setTheme] = useState<Theme>(defaults); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [saved, setSaved] = useState(false); const [published, setPublished] = useState(false); const [error, setError] = useState('');
  useEffect(() => { (async () => { const s=createClient(); const {data}=await s.from('storefronts').select('theme_settings,status').eq('organization_id',organizationId).maybeSingle(); const incoming=(data?.theme_settings||{}) as Partial<Theme>; setTheme({...defaults,...incoming}); setPublished(data?.status==='published'); setLoading(false); })(); }, [organizationId]);
  const update=<K extends keyof Theme>(key:K,value:Theme[K])=>setTheme(t=>({...t,[key]:value}));
  async function save(){setSaving(true);setSaved(false);setError('');const {error:e}=await createClient().from('storefronts').update({theme_settings:theme}).eq('organization_id',organizationId);if(e)setError(e.message);else{setSaved(true);setTimeout(()=>setSaved(false),1800)}setSaving(false)}
  async function togglePublish(){setSaving(true);setError('');const next=!published;const {error:e}=await createClient().from('storefronts').update({status:next?'published':'draft'}).eq('organization_id',organizationId);if(e)setError(e.message);else setPublished(next);setSaving(false)}
  if(loading)return <div className="panel empty">A carregar o personalizador…</div>;
  const previewBg=theme.mode==='dark'?'#202020':theme.mode==='soft'?'#f2eee7':'#fbfaf8'; const previewText=theme.mode==='dark'?'#fff':'#171717'; const cardBg=theme.mode==='dark'?'#2b2b2b':'#fff';
  return <div><div className="page-heading"><div><h1>Personalizar vitrine</h1><p>Escolhe o aspecto da tua loja, guarda e abre a pré-visualização.</p></div><div style={{display:'flex',gap:8}}><a className="view-store" href={storefrontPath}><Eye size={16}/> Ver vitrine</a><button className="primary" onClick={togglePublish} disabled={saving}>{published?'Despublicar':'Publicar'}</button></div></div>{error&&<div className="panel empty" style={{color:'#ff9e94'}}>{error}</div>}<div className="editor"><aside><h3>Personalização</h3><div className="editor-section expanded"><div className="editor-section-head"><span>Tema</span><Type size={14}/></div><div className="editor-section-body"><label>Modo<select value={theme.mode} onChange={e=>update('mode',e.target.value as Theme['mode'])}><option value="light">Claro</option><option value="soft">Suave</option><option value="dark">Escuro</option></select></label><label>Cor de destaque<input type="color" value={theme.accent} onChange={e=>update('accent',e.target.value)}/></label><label>Arredondamento<select value={theme.radius} onChange={e=>update('radius',e.target.value as Theme['radius'])}><option value="small">Pequeno</option><option value="medium">Médio</option><option value="large">Grande</option></select></label></div></div><div className="editor-section expanded"><div className="editor-section-head"><span>Layout</span><LayoutGrid size={14}/></div><div className="editor-section-body"><div className="seg"><button className={theme.layout==='grid'?'on':''} onClick={()=>update('layout','grid')}>Grelha</button><button className={theme.layout==='list'?'on':''} onClick={()=>update('layout','list')}>Lista</button></div><label>Mostrar preços<input type="checkbox" checked={theme.showPrices} onChange={e=>update('showPrices',e.target.checked)}/></label></div></div><button className="primary" onClick={save} disabled={saving} style={{width:'100%',justifyContent:'center'}}>{saved?<><Check size={16}/> Guardado</>:<><Save size={16}/> {saving?'A guardar…':'Guardar alterações'}</>}</button></aside><div className="editor-preview"><div className="preview-toolbar"><span>Pré-visualização</span><span style={{marginLeft:'auto'}}>{published?'Publicado':'Rascunho'}</span></div><div className="store-mini" style={{background:previewBg,color:previewText,borderRadius:theme.radius==='small'?8:theme.radius==='large'?24:16}}><div style={{padding:'34px 28px',textAlign:'center',borderBottom:'1px solid #0001'}}><div className="monogram" style={{margin:'0 auto 10px',background:theme.accent,color:'#fff'}}>P</div><h2 style={{margin:0,fontFamily:'Playfair Display,serif'}}>A tua vitrine</h2><p style={{opacity:.65,fontSize:12}}>Os teus produtos publicados aparecem aqui.</p></div><div style={{display:theme.layout==='grid'?'grid':'block',gridTemplateColumns:'repeat(2,1fr)',gap:12,padding:18}}>{[1,2,3].map(i=><div key={i} style={{background:cardBg,border:'1px solid #0001',borderRadius:theme.radius==='small'?8:theme.radius==='large'?20:14,padding:16,marginBottom:theme.layout==='list'?10:0}}><div style={{height:100,borderRadius:10,background:`linear-gradient(135deg, ${theme.accent}22, #0000000d)`,marginBottom:12}}/><strong>Produto {i}</strong>{theme.showPrices&&<div style={{marginTop:6,fontWeight:700}}>Preço</div>}</div>)}</div></div></div></div></div>;
}
